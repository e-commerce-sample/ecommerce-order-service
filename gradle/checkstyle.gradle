apply plugin: 'checkstyle'

checkstyle {
    sourceSets = [sourceSets.main]
    maxWarnings = 0
    ignoreFailures = false
}

tasks.withType(Checkstyle) {
    exclude 'com/ecommerce/order/common/configuration/OrderProperties.java'
    exclude 'com/ecommerce/order/spike/**'
}


